# Cria o ambiente (se ainda não criou)
python3 -m venv venv
# Ativa o ambiente (Obrigatório toda vez que abrir o terminal)
source venv/bin/activate
# Agora instale as ferramentas necessárias
pip install pandas openpyxl numpy